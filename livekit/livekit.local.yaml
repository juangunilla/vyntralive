port: 7880
log_level: info

rtc:
  tcp_port: 7881
  udp_port: 7882
  use_external_ip: false

redis:
  address: redis:6379

keys:
  devkey: secret

ingress:
  rtmp_base_url: "rtmp://localhost:1935/live"
  whip_base_url: "http://localhost:8080/whip"
